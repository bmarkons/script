version: v1.0
name: First pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: RSpec
    task:
      prologue:
        commands:
          - checkout
      jobs:
        - name: rspec
          commands:
            - docker build -t script .
            - docker run script rspec_booster --job 1/3
